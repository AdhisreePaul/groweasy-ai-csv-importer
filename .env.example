NODE_ENV=development

# Backend-only secret. Copy this value into apps/api/.env when AI integration is implemented.
AI_API_KEY=

# Public frontend setting. Values prefixed with NEXT_PUBLIC_ are visible in the browser.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
